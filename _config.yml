name: Kammerchor Innsbruck

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"

kramdown:
  header_offset: 1
